[tool.black]
line-length = 79
target-version = ["py37", "py38", "py39", "py310"]
include = '\.pyi?$'

[build-system]
# Defined by PEP 518
requires = [
  "setuptools>=45",
  "wheel",
]
# Defined by PEP 517
build-backend = "setuptools.build_meta"

